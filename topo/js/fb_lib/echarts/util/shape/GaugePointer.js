define(["require","zrender/shape/Base","zrender/tool/util","./normalIsCover"],function(require){function GaugePointer(options){Base.call(this,options)}var Base=require("zrender/shape/Base"),zrUtil=require("zrender/tool/util");GaugePointer.prototype={type:"gauge-pointer",buildPath:function(ctx,style){var r=style.r,width=style.width,angle=style.angle,x=style.x-Math.cos(angle)*width*(width>=r/3?1:2),y=style.y+Math.sin(angle)*width*(width>=r/3?1:2);angle=style.angle-Math.PI/2;ctx.moveTo(x,y);ctx.lineTo(style.x+Math.cos(angle)*width,style.y-Math.sin(angle)*width);ctx.lineTo(style.x+Math.cos(style.angle)*r,style.y-Math.sin(style.angle)*r);ctx.lineTo(style.x-Math.cos(angle)*width,style.y+Math.sin(angle)*width);ctx.lineTo(x,y)},getRect:function(style){if(style.__rect)return style.__rect;var width=2*style.width,xStart=style.x,yStart=style.y,xEnd=xStart+Math.cos(style.angle)*style.r,yEnd=yStart-Math.sin(style.angle)*style.r;style.__rect={x:Math.min(xStart,xEnd)-width,y:Math.min(yStart,yEnd)-width,width:Math.abs(xStart-xEnd)+width,height:Math.abs(yStart-yEnd)+width};return style.__rect},isCover:require("./normalIsCover")};zrUtil.inherits(GaugePointer,Base);return GaugePointer});